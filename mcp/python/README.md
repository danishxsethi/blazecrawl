# blazecrawl-mcp (Python)

MCP server exposing BlazeCrawl `scrape` / `map` / `crawl` tools over stdio.

```json
{
  "mcpServers": {
    "blazecrawl": {
      "command": "blazecrawl-mcp",
      "env": { "BLAZECRAWL_API_KEY": "blz_local_..." }
    }
  }
}
```
